NODE_ENV=production # production | development | test
PORT=3000

# Database Configuration - Amazon RDS PostgreSQL
# Este proyecto usa dos bases de datos separadas en Amazon RDS

# Common Database URL - Base de datos compartida entre localidades
# Contiene: Admin, Session, Locality, LegalEntity, Company
# Local development:
# DATABASE_COMMON_URL=postgresql://postgres:postgres@localhost:5432/common-reports-urgara?schema=public
# Amazon RDS (producción):
# IMPORTANTE: Agregar sslmode=no-verify para evitar error P1010 con Prisma 7 + @prisma/adapter-pg en AWS RDS
DATABASE_COMMON_URL=postgresql://username:password@your-rds-endpoint.region.rds.amazonaws.com/common-reports-urgara?sslmode=no-verify&schema=public

# Locality Database URL - Base de datos específica por localidad (template)
# Contiene: WorkShift, Worker, WorkerAssignment, Terminal
# IMPORTANTE:
# - {locality} es un placeholder que será reemplazado automáticamente por el campo "databaseName" de cada Locality
# - La API cargará automáticamente TODAS las localidades activas al iniciar y creará conexiones a sus respectivas DBs
# - El routing a la DB correcta se hace por localityId del admin autenticado (desde JWT)
# - Agregar sslmode=no-verify para AWS RDS (ver DATABASE_COMMON_URL)
# Ejemplos de nombres en DB: mardelplata-reports-urgara, buenosaires-reports-urgara
# Local development:
# DATABASE_LOCALITY_URL=postgresql://postgres:postgres@localhost:5432/{locality}?schema=public
# Amazon RDS (producción):
DATABASE_LOCALITY_URL=postgresql://username:password@your-rds-endpoint.region.rds.amazonaws.com/{locality}?sslmode=no-verify&schema=public

# JWT Configuration
JWT_SECRET_CLIENT=your_jwt_secret_key_here_change_in_production
JWT_SECRET_REFRESH=your_jwt_refresh_secret_key_here_change_in_production
# COOKIE
COOKIE_SECRET=your_cookie_secret
# Application Configuration
CORS_ORIGIN=http://localhost:3001

# Default Admin User (Optional - Only used on first initialization)
# These credentials will be used to create the first admin user
# IMPORTANT: Change the password after first login!
DEFAULT_ADMIN_DNI=12345678
DEFAULT_ADMIN_PASSWORD=Admin123!
DEFAULT_ADMIN_NAME=Dynnamo
DEFAULT_ADMIN_SURNAME=Sistema
DEFAULT_ADMIN_ROLE=1  # 1 = ADMIN, 5 = LOCAL, 10 = ONLY_READ
